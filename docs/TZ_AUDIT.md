# Аудит требований

Проверено по `specifications.xmind` и `finances.xmind` 09.08.2026. Прямые уточнения имеют приоритет над исходными картами: email полностью исключён, web-админки нет, каталог редактируется через Excel, административные операции выполняются ботом.

Статусы: **готово** — сценарий реализован; **частично** — есть основа, но не весь пользовательский путь; **не сделано** — рабочего сценария пока нет.

## Клиентская часть

| Пункт ТЗ | Статус | Что есть / чего не хватает |
|---|---|---|
| 1. Telegram-бот | Частично | Точка входа, меню и базовая админская сводка готовы. Уведомления по заказам и информация об оплате ещё не реализованы. |
| 2. Приветствие | Готово | `/start` создаёт клиента и выводит приветствие со всеми четырьмя inline-кнопками. |
| 3. Mini App | Частично | Есть каталог, поиск, карточки, избранное, недавние, корзина и профиль. Заказы, отзывы, коины, промокоды, рефералы и встроенная поддержка ещё не готовы. |
| 5. Каталог | Готово | Товары, поиск, категории, фильтр цены, популярные, новинки и рекомендации выводятся. |
| 6. Категории | Частично | Категории динамически создаются из Excel и выводятся в каталоге. Отдельные изображение, скрытие и удаление категории через Excel пока не предусмотрены. |
| 7. Товар | Готово | Создание и изменение основных данных, фото, цен, скидки и категории выполняются через `assets/products.xlsx`; пропавший SKU скрывается. |
| 8. Скидка товара | Готово | Старая и новая цены берутся из Excel и выводятся в каталоге и карточке. Применение к заказу появится вместе с checkout. |
| 9. Карточка товара | Частично | Фото, данные, цены, категория и добавление в корзину есть. Отзывы и рабочий запрос наличия отсутствуют. |
| 10. Популярные | Частично | Есть ручной признак из Excel и счётчики просмотров/корзины. Автоматическое ранжирование по метрикам и покупкам не подключено. |
| 11. Новинки | Готово | Метка действует семь дней с даты создания товара. |
| 12. Недавно просмотренные | Готово | Просмотр записывается автоматически, последние товары выводятся отдельным списком. |
| 13. Избранное | Готово | Добавление, удаление, просмотр списка и переход в карточку работают. |
| 14. Узнать наличие | Не сделано | Есть модель запроса, но нет клиентской кнопки и полного admin flow ответа. |
| 15. Корзина | Частично | Добавление, удаление, количество и итоговая сумма выводятся. Нет изменения количества, выбора скидки, промокода, коинов и checkout. |
| 16. Подтверждение количества | Не сделано | Есть поля модели наличия, пользовательский и административный сценарии отсутствуют. |
| 17. Регистрация | Частично | Клиент создаётся по Telegram-сессии, имя и фамилия подставляются один раз и редактируются; дата рождения и `phone_number` сохраняются. Заполнение ещё не блокирует первую покупку. Email намеренно исключён. |
| 18. Внутренний ID | Готово | Постоянный `Customer.id` генерируется БД; Telegram ID хранится отдельно в `user_id`. |
| 19. Карточка клиента | Частично | Основные модели клиента, заказов, коинов, рефералов и поддержки есть. Единого административного сценария просмотра карточки нет. |
| 20. Профиль | Частично | Основные личные поля, Telegram username и внутренний ID выводятся и редактируются. Заказы, коины, скидка, рефералы и поддержка пока не выведены. |
| 21. Мои заказы | Не сделано | Модели есть, клиентского списка и статусов нет. |
| 22. Коины | Частично | Баланс и журнал смоделированы, пользовательский сценарий и применение к заказу отсутствуют. |
| 23–24. Скидка и рефералы | Частично | Поля скидки, код и модель связи есть. Deep link, проверка регистрации/подписки и начисления не реализованы; email по уточнению не используется. |
| 25–26. Промокод и выбор скидки | Частично | Модель и чистый расчёт взаимоисключающих скидок есть, но они не подключены к корзине и заказу. |
| 27–28. Промокоды и партнёры | Не сделано | Нет Telegram admin flow создания промокодов, карточки партнёра и статистики. |
| 29. Оформление и оплата | Не сделано | Модели заказа и снимков есть, сквозного сценария оформления и проверки оплаты нет. |
| 30. Учёт промокода после оплаты | Не сделано | Правило зафиксировано, но событие подтверждения оплаты ещё не реализовано. |
| Отзывы | Не сделано | Пока выводится информационная заглушка. |
| Поддержка | Частично | Бот ведёт по внешней Telegram-ссылке; встроенных обращений и истории нет. |

## Финансы

| Блок ТЗ | Статус | Что есть / чего не хватает |
|---|---|---|
| Расчёт продажи и варианты скидок | Частично | Чистая функция расчёта учитывает товарную скидку, один из вариантов скидки, коины, опт и доли. Она ещё не вызывается из заказа. |
| Доли Дианы и Булата | Частично | В Excel и моделях сохраняются владелец и процент, расчёт поддерживает распределение. Фиксации по оплаченному заказу нет. |
| Карточка оплаченной продажи | Частично | Поля снимков предусмотрены в `Order` и `OrderItem`, рабочего создания карточки нет. |
| Отчёты по периодам, товарам и людям | Не сделано | В боте есть только общая числовая сводка. Финансовые отчёты и Excel-экспорт отсутствуют. |
| Начисления и выплаты | Частично | Модель выплаты существует, начисление после статуса «Оплачено» и история в admin flow не реализованы. |
| Неизменяемая история | Частично | Структура snapshot-полей подготовлена, но момент фиксации оплаты ещё не реализован. |

## Что можно проверить сейчас

- запуск контейнера зависимостей `rewire`, применение Alembic-схемы и синхронизацию `assets/products.xlsx`;
- публичный Jinja2-каталог, поиск, фильтры, главную страницу и карточку товара;
- отклонение персональных API без Telegram `initData` и работу с корректно подписанным `initData` без cookie;
- создание клиента с `user_id`/`username`, первоначальное заполнение имён из Telegram и сохранение изменённых имён, даты рождения и `phone_number`;
- избранное, недавно просмотренные, корзину и профиль через персональные API;
- Telegram-команды и inline-кнопки при наличии реального токена и публичного HTTPS URL.

Нельзя проверить как готовые пользовательские сценарии пункты со статусом «не сделано». Production-проверка также требует PostgreSQL, реального Telegram-бота и HTTPS Mini App URL.
